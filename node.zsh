## node.js
# NODE_PATH=/usr/local/lib/node_modules
# NODE_PATH='/usr/local/lib/jsctags:${NODE_PATH}'
# export NODE_PATH

# Load NVM
if [ -s ~/.nvm/nvm.sh ]; then
  NVM_DIR=~/.nvm
  source ~/.nvm/nvm.sh
fi

# load node
# nvm use default
